# Optional. Base URL for the Sky Scout flight-search backend (FastAPI, exposes POST /search).
# Defaults to the deployed Render instance if unset.
VITE_RECOMMENDATIONS_API_URL=https://sky-scout-2lr6.onrender.com
